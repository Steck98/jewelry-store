# Jewelry Store

A Django portfolio project for a premium jewelry e-commerce website.

## Setup

1. Create and activate a virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Check the project configuration with `python manage.py check`.

The project currently contains only the initial Django structure. Application features have not yet been implemented.
